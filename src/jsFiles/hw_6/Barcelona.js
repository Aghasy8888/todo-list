import React from "react";

function Barcelona(props) {
  return (
    <div>
      <h4>
        {props.name} is a {props.country} football club
      </h4>
    </div>
  );
}

export default Barcelona;
